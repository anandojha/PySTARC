# p38 MAPK / SB203580 (protein-ligand complex)

## System

| Property               | Value                                                   |
|------------------------|---------------------------------------------------------|
| PDB                    | 1A9U (Wang et al., 1998)                                |
| Receptor               | p38 MAPK alpha, DFG-in conformation (5658 atoms, Q = -9 e) |
| Ligand                 | SB203580, type I inhibitor (27 atoms, Q = 0 e)          |
| b-surface              | 60.0 Angstrom                                           |
| Escape sphere          | 120.0 Angstrom                                          |
| Debye length           | 7.86 Angstrom (150 mM NaCl)                             |
| Reaction pairs         | 4 (MET106-NB1, LYS50-NC3, VAL102-FD3, THR103-FD3)      |
| Reaction cutoff        | 7.0 Angstrom (all pairs)                                |
| n_needed               | 2                                                       |
| Born desolvation       | enabled                                                 |
| Hydrodynamic radii     | auto                                                    |
| Trajectories           | 100,000                                                 |
| Experimental k<sub>on</sub> | 1.5 x 10<sup>7</sup> M<sup>-1</sup>s<sup>-1</sup> (Miao et al., 2018) |
| Browndye BD k<sub>on</sub> | 6.6 x 10<sup>7</sup> M<sup>-1</sup>s<sup>-1</sup> (Huang, Biophys J, 2021) |

## Files

| File                | Provided | Generated by setup.py | Description                                                                 |
|---------------------|----------|-----------------------|-----------------------------------------------------------------------------|
| `setup.py`          | yes      |                       | Automated setup: downloads PDB, extracts protein and ligand, parameterizes with ff14SB (protein) and GAFF2/AM1-BCC (ligand), generates PQR files, reaction criterion, and input files. |
| `run.sh`            | yes      |                       | Runs setup and BD simulation in one command.                                |
| `1A9U.pdb`          |          | yes (downloaded)      | Crystal structure from the PDB.                                             |
| `receptor.pqr`      |          | yes                   | Receptor (p38) PQR from ambpdb.                                             |
| `ligand.pqr`        |          | yes                   | Ligand (SB203580) PQR from ambpdb.                                          |
| `receptor.pdb`      |          | yes                   | Clean receptor PDB from tleap.                                              |
| `ligand.pdb`        |          | yes                   | Clean ligand PDB from tleap.                                                |
| `protein.prmtop`    |          | yes                   | Receptor AMBER topology.                                                    |
| `protein.rst7`      |          | yes                   | Receptor AMBER coordinates.                                                 |
| `ligand.prmtop`     |          | yes                   | Ligand AMBER topology.                                                      |
| `ligand.rst7`       |          | yes                   | Ligand AMBER coordinates.                                                   |
| `rxns.xml`          |          | yes                   | Reaction criterion with atom pairs and cutoff distances.                    |
| `input.xml`         |          | yes                   | PySTARC input file with all simulation parameters.                          |

## Setup and run

Pre-generated files are not included because the ligand parameterization (antechamber AM1-BCC) must be run at setup time. Run setup.py first:

```bash
conda activate PySTARC
module load cuda
cd examples/p38_mapk_sb203580
python setup.py
python ../../run_pystarc.py input.xml
```

Or in one command:

```bash
conda activate PySTARC
module load cuda
cd examples/p38_mapk_sb203580
bash run.sh
```

## Dependencies

In addition to the standard PySTARC dependencies, setup.py requires antechamber (from AmberTools, included in the PySTARC conda environment) to parameterize the ligand with GAFF2 force field and AM1-BCC partial charges.

## Output files

After the simulation completes, all results are written to `bd_sims/`.

| Output file              | Description                                                                                                                    |
|--------------------------|--------------------------------------------------------------------------------------------------------------------------------|
| `results.json`           | k<sub>on</sub>, P<sub>rxn</sub>, Wilson 95% CI, k<sub>b</sub>, D<sub>rel</sub>, wall time, and GPU info.                      |
| `convergence.json`       | Convergence analysis: SE, relative SE, Wilson CI, convergence verdict, and trajectory estimates for target precision.           |
| `trajectories.csv`       | Per-trajectory record: number of steps, starting pose, minimum distance reached, and number of returns from the escape sphere. |
| `encounters.csv`         | Binding encounter poses for reacted trajectories: final position, orientation, and contact distances.                          |
| `near_misses.csv`        | Trajectories that approached the reaction surface but escaped.                                                                 |
| `fpt_distribution.csv`   | First-passage times for reacted trajectories.                                                                                  |
| `pose_clusters.csv`      | Clustered binding poses from encounter geometries.                                                                             |
| `paths.npz`              | Full trajectory coordinates sampled at configurable intervals.                                                                 |
| `energetics.npz`         | Per-step energies and forces along trajectories.                                                                               |
| `radial_density.csv`     | Radial probability density as a function of distance from the receptor.                                                        |
| `angular_map.npz`        | Angular occupancy map (theta, phi) on the b-surface.                                                                           |
| `contact_frequency.csv`  | Per-pair contact frequencies for the reaction criterion atom pairs.                                                            |
| `milestone_flux.csv`     | Net flux across radial shells.                                                                                                 |
| `transition_matrix.npz`  | Radial shell-to-shell transition counts.                                                                                       |
| `p_commit.npz`           | Commitment probabilities at each radial shell.                                                                                 |

A timestamped log file (`pystarc_YYYYMMDD_HHMMSS.log`) is also written to `bd_sims/` containing the full simulation output.

## Reaction criterion

Four crystal-structure contacts between p38 and SB203580 define the reaction criterion:

| Pair | Receptor atom          | Ligand atom | Contact type        |
|------|------------------------|-------------|---------------------|
| 1    | MET106 N (hinge)       | NB1         | Hinge H-bond        |
| 2    | LYS50 NZ (catalytic)   | NC3         | Catalytic lysine     |
| 3    | VAL102 O (backbone)    | FD3         | Backbone carbonyl    |
| 4    | THR103 N (backbone)    | FD3         | Backbone amide       |

With `n_needed=2`, any two of the four contacts firing simultaneously counts as a reaction.

## Notes

- SB203580 is electrically neutral at physiological pH. Association is driven purely by diffusion and short-range interactions, not electrostatic steering.
- The His-tag residues (residues 1-3 in PDB 1A9U) are excluded from the receptor to avoid artifacts from the expression construct.
- The ligand is parameterized separately from the protein using antechamber (GAFF2 + AM1-BCC), then each molecule gets its own AMBER topology and PQR file.
- PQR files are generated from the AMBER topology via `cpptraj` and `ambpdb`.
- The geometry check reports that b + R<sub>max, lig</sub> = 68 Angstrom slightly exceeds the fine grid half-extent of 64 Angstrom. The Yukawa multipole fallback handles the overshoot for the outermost ligand atoms at the b-surface.
