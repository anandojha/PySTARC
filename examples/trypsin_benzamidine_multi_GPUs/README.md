# Trypsin-benzamidine (cluster single-GPU and multi-GPU via SLURM)

## System

Same trypsin-benzamidine protein-ligand complex as `trypsin_benzamidine/`, but demonstrating how to run PySTARC on an HPC cluster with SLURM. Two SLURM scripts are provided:

- `submit_SLURM_single_GPU.sh` - runs the entire simulation on one GPU
- `submit_SLURM_multi_GPUs.sh` - splits the simulation across 4 GPUs on one node and combines the results

<table width="100%">
<thead><tr><th align="left">Property</th><th align="left">Value</th></tr></thead>
<tbody>
<tr><td>PDB</td><td>seekrflow manuscript (doi.org/10.1101/2025.08.13.669965)</td></tr>
<tr><td>Receptor</td><td>Trypsin (3220 atoms, Q = +6 e)</td></tr>
<tr><td>Ligand</td><td>Benzamidine (18 atoms, Q = +1 e)</td></tr>
<tr><td>b-surface</td><td>45.0 Å</td></tr>
<tr><td>Escape sphere</td><td>90.0 Å</td></tr>
<tr><td>Debye length</td><td>7.86 Å (150 mM NaCl)</td></tr>
<tr><td>Reaction pairs</td><td>10 polar contacts</td></tr>
<tr><td>Reaction cutoff</td><td>6.0-8.5 Å (per pair)</td></tr>
<tr><td>n_needed</td><td>4</td></tr>
<tr><td>max_dt</td><td>0 (no cap)</td></tr>
<tr><td>Born desolvation</td><td>enabled</td></tr>
<tr><td>Hydrodynamic radii</td><td>rec 22.5, lig 5.0 (Å)</td></tr>
<tr><td>Trajectories</td><td>10,000,000</td></tr>
<tr><td>Experimental k<sub>on</sub></td><td>2.9 × 10<sup>7</sup> M<sup>-1</sup>s<sup>-1</sup> (Votapka et al. 2022)</td></tr>
</tbody>
</table>

See [`../PARAMETERS.md`](../PARAMETERS.md) Section 2 for parameter selection rationale.

## Files

<table width="100%">
<thead><tr><th align="left">File</th><th align="left">Provided</th><th align="left">Generated by setup.py</th><th align="left">Description</th></tr></thead>
<tbody>
<tr><td><code>complex.pdb</code></td><td>yes</td><td>yes (downloaded)</td><td>Trypsin-benzamidine co-crystal structure.</td></tr>
<tr><td><code>complex.prmtop</code></td><td>yes</td><td>yes</td><td>AMBER topology (ff14SB + GAFF2 with AM1-BCC).</td></tr>
<tr><td><code>setup.py</code></td><td>yes</td><td></td><td>Automated setup: generates receptor/ligand PQR, rxns.xml, and input.xml.</td></tr>
<tr><td><code>receptor.pqr</code></td><td>yes</td><td>yes</td><td>Trypsin PQR from ambpdb.</td></tr>
<tr><td><code>ligand.pqr</code></td><td>yes</td><td>yes</td><td>Benzamidine PQR from ambpdb.</td></tr>
<tr><td><code>rxns.xml</code></td><td>yes</td><td>yes</td><td>Reaction criterion with 10 polar contact pairs.</td></tr>
<tr><td><code>input.xml</code></td><td>yes</td><td>yes</td><td>PySTARC input file with all simulation parameters.</td></tr>
<tr><td><code>submit_SLURM_single_GPU.sh</code></td><td>yes</td><td></td><td>SLURM batch script for single-GPU runs.</td></tr>
<tr><td><code>submit_SLURM_multi_GPUs.sh</code></td><td>yes</td><td></td><td>SLURM batch script for 4-GPU runs on one node.</td></tr>
</tbody>
</table>

## Prerequisites

Before submitting either script, set `PYSTARC_ROOT` inside the SLURM script to point to the PySTARC installation. Both scripts default to `$HOME/PySTARC`. If the installation is elsewhere, edit the line:

```bash
PYSTARC_ROOT=$HOME/PySTARC
```

Both scripts activate the `PySTARC` conda environment automatically via `conda shell.bash hook`.

## Single-GPU SLURM run

This run uses one GPU, 32 CPU threads, and a 4h walltime. It submits one job that runs setup and the full BD simulation sequentially.

```bash
cd examples/trypsin_benzamidine_multi_GPUs
sbatch submit_SLURM_single_GPU.sh
```

Resource request:

<table width="100%">
<thead><tr><th align="left">Resource</th><th align="left">Value</th></tr></thead>
<tbody>
<tr><td>Partition</td><td>gpu</td></tr>
<tr><td>GPUs</td><td>1</td></tr>
<tr><td>CPU threads</td><td>32</td></tr>
<tr><td>Memory</td><td>128 GB</td></tr>
<tr><td>Walltime</td><td>4 hours</td></tr>
</tbody>
</table>

Output files are written to `bd_sims/` in the example directory.

## Multi-GPU SLURM run

This run uses four GPUs on one node, each running 1/4 of the trajectories in parallel. The `multi_GPU_runs.py` splitter generates four `bd_sims/bd_{1..4}/` subdirectories with symlinked DX files and split random seeds. After all four finish, `combine_data.py` pools the results into a single `bd_sims/combined_results.json`.

```bash
cd examples/trypsin_benzamidine_multi_GPUs
sbatch submit_SLURM_multi_GPUs.sh
```

Resource request:

<table width="100%">
<thead><tr><th align="left">Resource</th><th align="left">Value</th></tr></thead>
<tbody>
<tr><td>Partition</td><td>gpu</td></tr>
<tr><td>GPUs</td><td>4</td></tr>
<tr><td>Tasks per node</td><td>4</td></tr>
<tr><td>CPU per task</td><td>8</td></tr>
<tr><td>Memory</td><td>128 GB</td></tr>
<tr><td>Walltime</td><td>4 hours</td></tr>
</tbody>
</table>

Workflow:

1. Cleans previous bd_sims and stale output files.
2. Runs `setup.py` to regenerate PQR, rxns.xml, input.xml.
3. Splits input.xml into 4 parts via `multi_GPU_runs.py`.
4. Launches 4 parallel BD processes, one per GPU (`CUDA_VISIBLE_DEVICES=0..3`).
5. Waits for all 4 to finish.
6. Runs `combine_data.py` to pool results.

Output structure:

```
bd_sims/
  bd_1/                      GPU 0 results
  bd_2/                      GPU 1 results
  bd_3/                      GPU 2 results
  bd_4/                      GPU 3 results
  combined_results.json      Pooled k_on, P_rxn, CI from all 4 GPUs
```

## Output files

After the simulation completes, all results are written to `bd_sims/`. Single-GPU runs produce the standard file set directly in `bd_sims/`. Multi-GPU runs produce one set of files per `bd_*/` subdirectory plus a pooled `combined_results.json`.

<table width="100%">
<thead><tr><th align="left">Output file</th><th align="left">Description</th></tr></thead>
<tbody>
<tr><td><code>results.json</code></td><td>k<sub>on</sub>, P<sub>rxn</sub>, Wilson 95% CI, k<sub>b</sub>, D<sub>rel</sub>, wall time, and GPU info (per sub-run).</td></tr>
<tr><td><code>combined_results.json</code></td><td>Pooled k<sub>on</sub> and CI across all GPU sub-runs (multi-GPU only).</td></tr>
<tr><td><code>convergence.json</code></td><td>Convergence analysis: SE, relative SE, Wilson CI, convergence verdict, and trajectory estimates for target precision.</td></tr>
<tr><td><code>trajectories.csv</code></td><td>Per-trajectory record: number of steps, starting pose, minimum distance reached, and number of returns from the escape sphere.</td></tr>
<tr><td><code>encounters.csv</code></td><td>Binding encounter poses for reacted trajectories: final position, orientation, and contact distances.</td></tr>
<tr><td><code>near_misses.csv</code></td><td>Trajectories that approached the reaction surface but escaped.</td></tr>
<tr><td><code>fpt_distribution.csv</code></td><td>First-passage times for reacted trajectories.</td></tr>
<tr><td><code>pose_clusters.csv</code></td><td>Clustered binding poses from encounter geometries.</td></tr>
<tr><td><code>paths.npz</code></td><td>Full trajectory coordinates sampled at configurable intervals.</td></tr>
<tr><td><code>energetics.npz</code></td><td>Per-step energies and forces along trajectories.</td></tr>
<tr><td><code>radial_density.csv</code></td><td>Radial probability density as a function of distance from the receptor.</td></tr>
<tr><td><code>angular_map.npz</code></td><td>Angular occupancy map (theta, phi) on the b-surface.</td></tr>
<tr><td><code>contact_frequency.csv</code></td><td>Per-pair contact frequencies for the reaction criterion atom pairs.</td></tr>
<tr><td><code>milestone_flux.csv</code></td><td>Net flux across radial shells.</td></tr>
<tr><td><code>transition_matrix.npz</code></td><td>Radial shell-to-shell transition counts.</td></tr>
<tr><td><code>p_commit.npz</code></td><td>Commitment probabilities at each radial shell.</td></tr>
</tbody>
</table>

A timestamped log file (`pystarc_YYYYMMDD_HHMMSS.log`) is also written to `bd_sims/` containing the full simulation output.

## Adjusting trajectory count and walltime

Default is 10,000,000 trajectories across 4 GPUs (2.5M per GPU). To change:

1. Edit `setup.py` and modify `N_TRAJECTORIES`.
2. Adjust `--time=4:00:00` in the SLURM script based on expected walltime.
3. For longer runs, increase to `--time=12:00:00` or `--time=24:00:00`.

## Notes

- The multi-GPU splitter generates independent random seeds for each GPU so the four sub-simulations are statistically independent.
- The `bd_sims/bd_*/input.xml` files reference the parent PQR and DX files rather than copying them, saving disk space.
- For interactive debugging, run one GPU directly: `CUDA_VISIBLE_DEVICES=0 python ../../run_pystarc.py input.xml`.
- `PYSTARC_ROOT` must be set correctly inside the SLURM script before submission. The default `$HOME/PySTARC` assumes PySTARC is cloned to your home directory.
